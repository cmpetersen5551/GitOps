apiVersion: v1
kind: Service
metadata:
  name: eplustv-headless
  namespace: live
  labels:
    app: eplustv
spec:
  type: ClusterIP
  clusterIP: None  # Headless
  selector:
    app: eplustv
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
