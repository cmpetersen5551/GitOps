apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-media-nfs
  namespace: live
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  storageClassName: nfs-unraid
  volumeName: pv-nfs-media-live
