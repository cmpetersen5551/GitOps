apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dispatcharr
  namespace: live
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: http
spec:
  ingressClassName: traefik
  rules:
    - host: dispatcharr.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dispatcharr
                port:
                  number: 9191
