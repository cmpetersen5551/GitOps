apiVersion: v1
kind: Service
metadata:
  name: teamarr-headless
  namespace: live
  labels:
    app: teamarr
spec:
  type: ClusterIP
  clusterIP: None  # Headless
  selector:
    app: teamarr
  ports:
    - port: 9195
      targetPort: 9195
      protocol: TCP
      name: http
