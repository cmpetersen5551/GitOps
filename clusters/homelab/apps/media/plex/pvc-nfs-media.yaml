apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs-media
  namespace: media
  labels:
    app.kubernetes.io/part-of: plex
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: pv-nfs-media
  resources:
    requests:
      storage: 1Ti
