apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs-streaming-media
  namespace: media
  labels:
    app.kubernetes.io/part-of: plex
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: pv-nfs-streaming-media
  resources:
    requests:
      storage: 10Gi
