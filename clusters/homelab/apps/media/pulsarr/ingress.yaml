apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pulsarr
  namespace: media
spec:
  ingressClassName: traefik
  rules:
    - host: pulsarr.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pulsarr
                port:
                  number: 80
