apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-eplustv
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-simple
  resources:
    requests:
      storage: 5Gi
