apiVersion: v1
kind: Service
metadata:
  name: sonarr-headless
  namespace: media
  labels:
    app: sonarr
spec:
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: sonarr
  ports:
    - port: 8989
      targetPort: 8989
      protocol: TCP
      name: http
