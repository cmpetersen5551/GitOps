apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clusterplex-dfs-nfs
  namespace: media
  labels:
    app.kubernetes.io/name: clusterplex-dfs-nfs
    app.kubernetes.io/part-of: clusterplex
spec:
  volumeName: clusterplex-dfs-nfs-pv
  accessModes:
    - ReadWriteMany
  storageClassName: ""  # Manual binding to PV
  resources:
    requests:
      storage: 100Gi
