---
apiVersion: seaweed.seaweedfs.com/v1
kind: Seaweed
metadata:
  name: seaweedfs
  namespace: seaweedfs
spec:
  image: chrislusf/seaweedfs:latest
  
  # Master servers configuration
  master:
    replicas: 3
    defaultReplication: "010"  # Bootstrap with single-node replication, upgrade to 032 once volume servers are healthy
    volumeSizeLimitMB: 1024
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi

  # Simple volume server configuration (will support all nodes)
  volume:
    replicas: 3
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi
      storage: 100Gi
    # Node selector for volume servers
    nodeSelector:
      sw-volume: "true"

  # Filer server configuration
  filer:
    replicas: 1
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi
    config: |
      [leveldb2]
      enabled = true
      dir = "/data/filerldb2"
