apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kadalu-operator
  namespace: flux-system
spec:
  interval: 1h
  chart:
    spec:
      chart: kadalu-operator
      version: "0.8.0"
      sourceRef:
        kind: HelmRepository
        name: kadalu-repo
        namespace: flux-system